# 十二、大数据前端可视化高阶

大数据可视化不只是画几个 ECharts 图表，而是面对**千万乃至亿级数据点的实时渲染、交互和分析**。核心矛盾是：浏览器只有有限的 GPU/CPU 资源，而数据是无限的——如何用有限的资源呈现尽可能多的数据洞察。

---

## 核心挑战

### 1. 地理 GIS 引擎

地理信息系统（GIS）是可视化中最复杂的分支之一。

- **瓦片地图加载**：
  - 地图不是一张大图，而是金字塔式的瓦片层级（Zoom Level 0~20+）。
  - 每个瓦片 256x256 px，当前视口需要加载约 6-12 个瓦片。
  - 瓦片服务器（Tile Server）的选择：Mapbox（商业）、OpenStreetMap（免费）、自建（MBTiles + 服务化）。

- **矢量地图 vs 栅格地图**：
  - 栅格瓦片：预渲染的 PNG 图片，加载快但无法动态改变样式。
  - 矢量瓦片（MVT/PMTiles）：发送几何数据（点/线/面），前端实时渲染。优势：一套数据可无限换样式（白天/黑夜模式）、可查询交互。
  - 矢量瓦片渲染引擎：MapLibre GL JS（开源，Mapbox GL JS 的分支）。

- **路网拓扑与空间计算**：
  - 路网数据的图结构：节点（路口）+ 边（路段）+ 权重（距离/时间）。
  - 最短路径算法（Dijkstra/A*）在前端的轻量级实现。
  - 地理围栏：判断点是否在多边形内。Ray Casting 算法。

- **坐标系统**：
  - EPSG:4326（WGS-84，GPS 原始）→ EPSG:3857（Web Mercator，百度/高德/Google 使用）。
  - 不同坐标系之间转换需要 7 参数 Helmert 变换或简化版 4 参数变换。

### 2. 时序大数据渲染

监控大盘、股票行情、IoT 传感器数据——都是高密度时序数据。

- **数据降采样（Downsampling）**：
  - 千万级原始数据点不能全部送到 Canvas 渲染（像素就那么多）。
  - Largest Triangle Three Buckets (LTTB) 算法：在保留趋势特征的前提下将 N 个点降采样为 M 个（M = 可视区域像素宽度）。
  - 降采样必须保留极值（金融图表的最高/最低价）、异常点（监控的突刺）。

- **滑动窗口**：
  - 内存中只保留当前窗口的数据（如最近 1 小时），窗口外数据卸载。
  - 用户滑动时间轴时动态加载对应时间窗口的数据。

- **流式实时推送**：
  - 新的时序数据点持续到来（WebSocket 推送）→ 增量更新 Canvas，不重绘全部。
  - 缓冲区累积：每 100ms 收集一次数据点，批量更新（避免每个点触发一次重绘）。

- **多轴与多指标**：
  - 同一图表显示多条时序线，Y 轴量纲不同（温度 + 湿度 + 电压）。
  - 多 Y 轴布局、动态 Y 轴范围（auto-scale 防突刺拉扁其他曲线）。

### 3. 拓扑图 / 流程图 / 架构图引擎

节点和连线构成的图结构有自己的布局和交互挑战。

- **自动布局算法**：
  - **力导向布局（Force-directed）**：模拟物理弹簧 + 斥力，迭代直到能量最小化。适合社交网络图、知识图谱。
  - **分层布局（Layered / Sugiyama）**：有向图按层级排列，最小化交叉边。适合架构图、组织架构。
  - **环形布局（Circular）**：节点排列在圆上，边穿过圆心。适合展示环形依赖。
  - **网格布局（Grid）**：节点按二维矩阵排列。

- **交互拾取（Hit Testing）**：
  - 点击 Canvas 上的节点 → 需要快速判断点击到了哪个节点。
  - GPU 拾取：为每个节点渲染不同颜色 → 读取点击位置的颜色 → 查颜色到节点的映射（Color Picking）。
  - 数学拾取：用 R-Tree / 四叉树空间索引 → 找到点击位置附近的节点 → 逐个判断点是否在节点形状内。

- **大规模图渲染**：
  - 10万+ 节点图的渲染瓶颈：布局计算 O(n²) 不可行。
  - 使用 WebGL/WebGPU 的 GPU 布局计算（Compute Shader 并行迭代）。
  - 交互式缩放：远距离显示聚类节点（Cluster），拉近后展开子节点。

### 4. 3D 空间数据大屏

城市数字孪生、工业数字孪生需要将 3D 模型和实时数据叠加。

- **3D 场景搭建**：
  - Three.js / Babylon.js 作为渲染引擎。
  - 模型加载：GLTF/GLB 格式（3D Tiles 用于大范围城市模型）。
  - 材质系统：PBR（物理基础渲染）材质模拟真实光照。

- **数据绑定**：
  - 3D 模型上的热点标记：数据变化 → 模型颜色/高度/标签动态变化。
  - 动画：摄像机漫游、设备运行动画（如工厂流水线的机械臂动作）。

- **3D 交互拾取**：
  - Raycasting：从摄像机发出一条射线 → 检测与哪些模型相交。
  - Three.js `Raycaster` 类提供实现，但大量模型时性能差 → 需要用 Bounding Volume Hierarchy (BVH) 加速。

- **性能优化**：
  - LOD（Level of Detail）：根据模型到摄像机的距离切换高低精度模型。
  - 实例化渲染：重复物体（路灯、树木、建筑窗户）用一个 draw call 渲染。
  - 遮挡剔除（Occlusion Culling）：被遮挡的模型不渲染。

---

## 深入学习路线

```
入门（1-2月）
├── ECharts、D3.js 基础：数据绑定、比例尺、布局
├── 常见图表实现：折线/柱状/饼图/散点图
├── Canvas/SVG 渲染对比与选型
└── 最小可视化 Dashboard

进阶（3-4月）
├── 大数据渲染：WebGL（Three.js/ECharts GL）
├── 时序数据处理：LTTB 降采样、滑动窗口、流式更新
├── 地理可视化：MapLibre GL JS + 矢量瓦片
└── 实战：实时监控大盘（WebSocket + 时序图 + 地图）

深入（6月+）
├── 自研图表引擎：渲染管线、交互层、主题系统
├── 3D 数字孪生：Three.js + 数据绑定 + 动画系统
├── 图布局算法实现：力导向、分层、环形
└── 图形学基础：坐标变换矩阵、裁剪算法、贝塞尔曲线
```

---

## 关键技术与开源项目

| 项目 | 用途 |
|------|------|
| [ECharts](https://echarts.apache.org/) | Apache 开源图表库，功能全面 |
| [D3.js](https://d3js.org/) | 数据驱动文档，底层可视化工具 |
| [Three.js](https://threejs.org/) | 3D 渲染引擎 |
| [Babylon.js](https://www.babylonjs.com/) | 微软 3D 渲染引擎，GIS 支持好 |
| [MapLibre GL JS](https://maplibre.org/) | 开源矢量地图渲染引擎 |
| [Cesium](https://cesium.com/) | 3D 地球 + GIS 引擎 |
| [Deck.gl](https://deck.gl/) | Uber 开源的大数据可视化框架 |
| [AntV](https://antv.antgroup.com/) | 蚂蚁集团可视化生态（G2/G6/L7） |
