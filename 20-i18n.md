# 二十一、前端国际化 i18n

国际化（i18n）不仅仅是"翻译文字"，而是涵盖布局方向、文化格式、字体排版、时区货币、性能优化的系统工程。一个真正国际化的应用，应该在 RTL 语言（阿拉伯语/希伯来语）下 UI 完美镜像，在日语下字体清晰可读，在德语下长单词不撑破布局。

---

## 核心挑战

### 1. RTL 右到左布局

阿拉伯语、希伯来语、波斯语等 RTL 语言需要**完全的布局镜像**，不仅仅是文字右对齐。

- **CSS Logical Properties（逻辑属性）**：
  - 用 `margin-inline-start` 替代 `margin-left`、`padding-inline-end` 替代 `padding-right`。
  - `start`/`end` 在 LTR 下映射为 `left`/`right`，在 RTL 下自动交换。
  - 同理：`border-inline-start`、`inset-inline-start`、`text-align: start`。
  - 支持度：所有现代浏览器均已支持，可放心使用。

- **UI 镜像布局**：
  - RTL 下不仅仅是文字右对齐——整个 UI 的视觉方向是镜像的。
  - 导航栏：LTR 时 Logo 在左、菜单在右 → RTL 时 Logo 在右、菜单在左。
  - 表单：LTR 时标签在左、输入框在右 → RTL 时标签在右、输入框在左。
  - 时间轴：LTR 从左到右 → RTL 从右到左。

- **图标/箭头方向翻转**：
  - 方向性图标需要翻转：右箭头（→）在 RTL 下变成左箭头（←），面包屑分隔符（/）在 RTL 下变成（\）。
  - 非方向性图标不应翻转：搜索图标（🔍）、设置齿轮不应翻转。
  - CSS 翻转：`transform: scaleX(-1)` 对方向性图标统一翻转。

- **混合 RTL + LTR 内容**：
  - 用户界面是 RTL（阿拉伯语），但用户输入的内容是 LTR（英文/数字）。
  - `dir="auto"`：浏览器根据第一个强方向字符自动判断文本方向。
  - `<bdi>`（Bidirectional Isolation）：隔离双向文本，防止 LTR 内容破坏 RTL 的布局流向。

### 2. 多语言文案管理

- **翻译 Key 体系设计**：
  - 命名空间化：`common.button.submit`、`checkout.payment.title`、`error.validation.required`。
  - 层级化：按页面/功能模块分组，方便查找和维护。
  - 避免 key 的语义漂移：`header.title` 如果只写"首页"，则不能复用于其他页面。

- **翻译平台集成**：
  - Crowdin / Lokalise / Phrase / Weblate：翻译协作平台，与 CI/CD 集成。
  - 工作流：开发者写代码 → 提取新的翻译 key → 上传到平台 → 翻译团队翻译 → 下载译文 → 打包部署。
  - 翻译记忆库（TM）：之前翻译过的相同/相似文案自动匹配，减少重复工作。

- **未翻译文案兜底**：
  - 翻译 key 缺失 → 显示 key 名称本身（如 `[checkout.payment.title]`）是最差的 UX。
  - 应显示默认语言的文案（如 `"Payment"`），或该 key 的英文版本作为 fallback。
  - `i18next` 支持 `fallbackLng` 配置兜底语言链（如 `['en', 'dev']` → 先找当前语言，再找 en，最后显示 key）。

- **格式化文案**（Interpolation）：
  - `"你好，{{name}}"` → `i18n.t('greeting', { name: '张三' })` → "你好，张三"。
  - 支持复数：`"{{count}} 个苹果"` → 1 个苹果 / 2 个苹果（不同语言复数规则不同）。

### 3. 文化敏感格式化

不同文化对数字、日期、货币的表示方式差异巨大。

- **Intl API（ECMAScript 国际化）**：
  - `Intl.NumberFormat(locale, { style: 'currency', currency: 'EUR' })` → `1.234,56 €`（德语区） vs `€1,234.56`（英语区）。
  - `Intl.DateTimeFormat(locale, { dateStyle: 'long' })` → "2026年5月6日"（中文） vs "May 6, 2026"（英文） vs "6. Mai 2026"（德文）。
  - `Intl.RelativeTimeFormat(locale)` → "3天前" / "in 3 days" / "vor 3 Tagen"。
  - `Intl.PluralRules(locale)`：每种语言的复数规则不同——英语 1 和 0/many；阿拉伯语有 6 种复数形式。

- **数字格式化注意**：
  - 千分位分隔符：`1,234.56`（英语） vs `1 234,56`（法语） vs `1.234,56`（德语）。
  - 货币符号位置：€100 vs 100 €（取决于语言，不取决于货币本身）。
  - 百分比：`50%` 还是 `50 %`（带空格）——法语要空格。

- **日期与时区**：
  - 服务器存 UTC → 客户端用 `Intl.DateTimeFormat` 按用户时区显示。
  - 日期输入组件（Date Picker）：需要考虑不同文化的周首日（周日 vs 周一）、日历类型（公历 vs 伊斯兰历 vs 佛历）。

- **性别化表述**：
  - 某些语言（阿拉伯语、法语）的形容词/动词需要随用户性别变化。
  - 文案设计应为中性或预留性别变量：`"欢迎回来，{{name}}"` 而非 `"Welcome back, sir"`。

### 4. CJK 及复杂文字排版

中日韩（CJK）文字有独特的排版挑战。

- **CJK 字体加载优化**：
  - CJK 字体文件极大（中文字体 > 5-15MB，对比英文 < 200KB）。
  - 子集化（Font Subsetting）：只包含用到的字符，缩减 80-90% 体积。工具：`glyphhanger`、`fonttools`。
  - `font-display: swap`：先用系统字体渲染 → 自定义字体加载完成后切换。
  - 分片加载：按 Unicode 范围拆分为多个字体文件（常用字 + 生僻字分片）。

- **竖排文字**：
  - 日文/中文传统排版支持竖排（`writing-mode: vertical-rl`）。
  - 竖排时所有布局属性交换：`width` ↔ `height`、`padding-left` ↔ `padding-top`。

- **注音（Ruby Annotation）**：
  - `<ruby>` 标签：在汉字上方或右边显示读音（注音/假名）。
  - `<ruby>漢字<rt>かんじ</rt></ruby>` → 汉字上方显示小字"かんじ"。

- **换行规则差异**：
  - CJK 文字可以在任意字符间换行（不需要空格分隔单词）。
  - `word-break: keep-all`：CJK 文本在字符间也保持不断行（依赖标点符号处换行）。
  - `line-break: strict`：更严格的中日韩换行规则（遵守特定的禁则）。

### 5. 动态国际化性能

- **语言包按需加载**：
  - 首屏只加载当前语言包 → 用户切换语言时懒加载新语言包。
  - 语言包大小：中大型应用的翻译文件可达数百 KB。按路由/模块拆分语言包（如 `homepage.en.json` + `dashboard.en.json`）。

- **SSR 下语言检测**：
  - 服务端通过 `Accept-Language` 请求头自动检测用户首选语言 → 返回对应语言的内容 HTML。
  - 也可以从 Cookie/URL（`/zh-CN/page`）中获取语言偏好。

- **翻译缓存与增量更新**：
  - 翻译文件做内容哈希 → 语言不变时长期缓存，不重复下载。
  - 增量更新：新增/修改的翻译 key 以补丁形式下发，而非重新下载整个语言包。

---

## 深入学习路线

```
入门（1-2月）
├── Intl API 基础：NumberFormat、DateTimeFormat、RelativeTimeFormat
├── i18n 框架选择：react-intl / vue-i18n / i18next
├── LTR/RTL 布局基础：CSS Logical Properties
└── 最小多语言 Demo：中英文切换

进阶（3-4月）
├── CSS Logical Properties 全面替换
├── 翻译工作流：Crowdin/Lokalise 集成 CI/CD
├── CJK 字体优化：子集化 + 分片加载
├── 动态语言包加载：按路由/模块拆分
└── 实战：完整多语言应用（含 RTL 支持）

深入（6月+）
├── 字体加载策略：Font Loading API、渐进式加载
├── 自研 i18n 引擎：ICU MessageFormat 解析 + AST 缓存
├── 复杂复数/性别规则：CLDR 数据处理
└── 多时区业务：日程系统、金融交易时间线
```

---

## 关键技术与开源项目

| 项目 | 用途 |
|------|------|
| [i18next](https://www.i18next.com/) | 通用 i18n 框架，React/Vue/Node 兼容 |
| [react-intl](https://formatjs.io/docs/react-intl/) | FormatJS React 国际化 |
| [vue-i18n](https://kazupon.github.io/vue-i18n/) | Vue 国际化 |
| [ICU MessageFormat](https://unicode-org.github.io/icu/userguide/format_parse/messages/) | Unicode 消息格式标准 |
| [CLDR](https://cldr.unicode.org/) | Unicode 通用语言数据仓库 |
| [fonttools](https://github.com/fonttools/fonttools) | 字体子集化/编辑工具 |
| [Crowdin](https://crowdin.com/) | 翻译协作平台 |
| [Lokalise](https://lokalise.com/) | 翻译管理平台 |
